import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class SignalRService {
  startConnection(): void {
    // SignalR connection placeholder — wired up when backend is available
  }

  stopConnection(): void {}
}
